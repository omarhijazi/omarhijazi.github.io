{
  "type": "module",
  "private": true,
  "dependencies": {
    "@fastly/js-compute": "^3.44.3"
  },
  "devDependencies": {
    "@fastly/cli": "^16.0.0"
  },
  "scripts": {
    "build": "js-compute-runtime src/index.js bin/main.wasm",
    "start": "fastly compute serve",
    "deploy": "fastly compute publish"
  }
}
